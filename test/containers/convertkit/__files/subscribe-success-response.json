{
  "subscription": {
    "id": 10101,
    "state": "active",
    "created_at": "{{now offset='0 milliseconds' format='yyyy-MM-dd\'T\'HH:mm:ssXXX'}}",
    "email_address": "{{jsonPath request.body '$.email'}}",
    "first_name": "{{jsonPath request.body '$.first_name'}}",
    "fields": {
      "source_page": "{{jsonPath request.body '$.fields.source_page'}}"
    }
  }
}
