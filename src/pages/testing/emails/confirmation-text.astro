---
import { generateConfirmationEmailText } from '@actions/newsletter/email/confirmationHtml'

export const prerender = false

const text = await generateConfirmationEmailText(
  'Jane',
  'https://www.webstackbuilders.com/newsletter/confirm/mock-token-123',
  '24 hours'
)
---

<pre>{text}</pre>
