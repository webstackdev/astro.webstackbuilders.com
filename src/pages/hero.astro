---
import BaseLayout from '@layouts/BaseLayout.astro'
const pageDescription = 'Hero Demo Pages'
const pageTitle = 'Hero Demo Pages'
const path = '/hero/'
---

<BaseLayout description={pageDescription} noindex={true} pageTitle={pageTitle} path={path}>

<section class="container mx-auto px-4 sm:px-6 lg:px-8" aria-labelledby="gemini-3">
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 pt-8 items-center">
    <div class="hidden lg:block lg:order-2"></div>
    <div class="order-2 lg:order-1 space-y-6">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-black text-content leading-none">
        Make production
        <span class="text-primary">boring</span> again
      </h1>
      <p class="text-lg md:text-xl text-content-offset max-w-xl leading-relaxed">
        I'm a platform engineer who fixes the reliability gaps that slow teams down: fragile releases, missing
        observability, and legacy systems nobody wants to touch.
      </p>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 pt-8 items-center">
    <div class="hidden lg:block lg:order-2"></div>
    <div class="order-2 lg:order-1 space-y-6">
      <div class="bg-content text-page-base rounded-2xl border border-content p-6">
        <div class="flex items-center justify-between mb-4">
          <p class="text-xs font-bold uppercase tracking-widest text-page-base/70">SLO Snapshot</p>
          <p class="text-xs text-page-base/70">Last 7 days</p>
        </div>
        <div class="grid grid-cols-3 gap-4">
          <div>
            <p class="text-2xl font-black">99.95%</p>
            <p class="text-xs text-page-base/70">Availability</p>
          </div>
          <div>
            <p class="text-2xl font-black">8m</p>
            <p class="text-xs text-page-base/70">MTTR</p>
          </div>
          <div>
            <p class="text-2xl font-black">0</p>
            <p class="text-xs text-page-base/70">Sev-1s</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
    <div class="hidden lg:block lg:order-2"></div>
    <div class="order-2 lg:order-1 space-y-8 py-8">
      <div class="bg-page-base-offset p-8 rounded-2xl border border-trim shadow-sm">
        <h3 class="text-lg font-bold text-content mb-4 flex items-center gap-2">
          <p class="text-xs font-bold text-content-offset uppercase tracking-widest mb-4">Blueprint</p>
        </h3>
        <ol class="grid gap-4 sm:grid-cols-2">
          <li class="flex items-start gap-3 bg-page-base p-3 rounded-lg border border-trim">
            <span class="font-mono text-primary font-bold text-xl">01.</span>
            <span class="text-sm text-content-offset leading-tight pt-1">
              <p class="font-bold text-content">Release Safety</p>
              <p class="text-sm text-content-offset">Progressive delivery, rollback paths, guardrails.</p>
            </span>
          </li>
          <li class="flex items-start gap-3 bg-page-base p-3 rounded-lg border border-trim">
            <span class="font-mono text-primary font-bold text-xl">02.</span>
            <span class="text-sm text-content-offset leading-tight pt-1">
              <p class="font-bold text-content">Observability</p>
              <p class="text-sm text-content-offset">Logs, metrics, traces that answer "why".</p>
            </span>
          </li>
          <li class="flex items-start gap-3 bg-page-base p-3 rounded-lg border border-trim">
            <span class="font-mono text-primary font-bold text-xl">03.</span>
            <span class="text-sm text-content-offset leading-tight pt-1">
              <p class="font-bold text-content">DX & Tooling</p>
              <p class="text-sm text-content-offset">Golden paths, templates, consistent environments.</p>
            </span>
          </li>
          <li class="flex items-start gap-3 bg-page-base p-3 rounded-lg border border-trim">
            <span class="font-mono text-primary font-bold text-xl">04.</span>
            <span class="text-sm text-content-offset leading-tight pt-1">
              <p class="font-bold text-content">Legacy Modernization</p>
              <p class="text-sm text-content-offset">Safe migration plans for hard systems.</p>
            </span>
          </li>
        </ol>
      </div>
    </div>
  </div>
</section>

<!-- ============================================================
     VARIATION 1: "Journey" style - Visual timeline approach
     ============================================================ -->
<section class="container mx-auto px-4 sm:px-6 lg:px-8 py-16" aria-labelledby="backstage-v1-title">
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
    <!-- Image placeholder -->
    <div class="order-2 lg:order-1">
      <div class="aspect-4/3 bg-page-base-offset rounded-2xl border-2 border-dashed border-trim flex items-center justify-center">
        <span class="text-content-offset text-sm">Image Placeholder</span>
      </div>
    </div>

    <!-- Content -->
    <div class="order-1 lg:order-2 space-y-6">
      <p class="text-xs font-bold text-primary uppercase tracking-widest">Internal Developer Platform</p>
      <h2 id="backstage-v1-title" class="text-3xl md:text-4xl font-black text-content leading-tight">
        From one app to a
        <span class="text-primary">self-service platform</span>
      </h2>
      <p class="text-lg text-content-offset leading-relaxed">
        I help teams adopt Backstage incrementally—starting with your most painful app, not a
        big-bang rewrite. We'll modernize CI/CD, add observability, and give developers a
        portal where they can deploy without waiting on ops.
      </p>

      <!-- Journey steps -->
      <div class="space-y-4 pt-4">
        <div class="flex items-start gap-4">
          <div class="w-10 h-10 rounded-full bg-primary text-page-base flex items-center justify-center font-bold shrink-0">1</div>
          <div>
            <p class="font-bold text-content">Discovery & Pilot App</p>
            <p class="text-sm text-content-offset">Map your pain points; pick one app to prove the pattern.</p>
          </div>
        </div>
        <div class="flex items-start gap-4">
          <div class="w-10 h-10 rounded-full bg-primary text-page-base flex items-center justify-center font-bold shrink-0">2</div>
          <div>
            <p class="font-bold text-content">Golden Path & Templates</p>
            <p class="text-sm text-content-offset">CI/CD, testing, cloud deploy—all baked into reusable scaffolds.</p>
          </div>
        </div>
        <div class="flex items-start gap-4">
          <div class="w-10 h-10 rounded-full bg-primary text-page-base flex items-center justify-center font-bold shrink-0">3</div>
          <div>
            <p class="font-bold text-content">Backstage Catalog & Self-Service</p>
            <p class="text-sm text-content-offset">Developers browse, create, and deploy without tickets.</p>
          </div>
        </div>
        <div class="flex items-start gap-4">
          <div class="w-10 h-10 rounded-full bg-primary text-page-base flex items-center justify-center font-bold shrink-0">4</div>
          <div>
            <p class="font-bold text-content">Expand Across the Portfolio</p>
            <p class="text-sm text-content-offset">Roll the pattern out to more apps as confidence grows.</p>
          </div>
        </div>
      </div>

      <a href="/contact" class="inline-flex items-center gap-2 bg-primary hover:bg-primary-offset text-page-base font-bold px-6 py-3 rounded-full transition-colors">
        Start the conversation
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
      </a>
    </div>
  </div>
</section>
<!--
  IMAGE PROMPT (Variation 1):
  A clean, modern isometric illustration showing a journey from chaos to order. On the left,
  a tangled mess of servers, pipelines, and confused developers. A glowing path leads right
  toward an organized Backstage portal interface with a catalog grid, happy developers
  browsing services, and one-click deploy buttons. Color palette: deep navy background,
  cyan/teal accent highlights, white and soft gray UI elements. Style: flat vector with
  subtle gradients, no outlines, tech-forward and approachable. Aspect ratio 4:3.
-->


<!-- ============================================================
     VARIATION 2: "Before/After" split-screen approach
     ============================================================ -->
<section class="bg-page-base-offset py-16" aria-labelledby="backstage-v2-title">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <header class="text-center max-w-3xl mx-auto mb-12">
      <p class="text-xs font-bold text-primary uppercase tracking-widest mb-4">Backstage IDP Consulting</p>
      <h2 id="backstage-v2-title" class="text-3xl md:text-4xl font-black text-content leading-tight mb-4">
        What if developers could <span class="text-primary">ship without waiting?</span>
      </h2>
      <p class="text-lg text-content-offset">
        I partner with engineering teams to stand up Backstage—Spotify's open-source developer
        portal—so your devs get self-service deploys, a living service catalog, and golden-path
        templates that actually work.
      </p>
    </header>

    <!-- Before/After cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-12">
      <!-- Before -->
      <div class="bg-page-base rounded-2xl border border-trim p-8 shadow-sm">
        <p class="text-xs font-bold text-red-600 uppercase tracking-widest mb-4">Before</p>
        <ul class="space-y-3">
          <li class="flex items-start gap-3">
            <span class="text-red-500 mt-1">✕</span>
            <span class="text-content-offset">Tickets to ops for every deploy</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-red-500 mt-1">✕</span>
            <span class="text-content-offset">Tribal knowledge locked in Slack threads</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-red-500 mt-1">✕</span>
            <span class="text-content-offset">Inconsistent CI/CD across projects</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-red-500 mt-1">✕</span>
            <span class="text-content-offset">Legacy apps nobody wants to touch</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-red-500 mt-1">✕</span>
            <span class="text-content-offset">"Who owns this service?" is a mystery</span>
          </li>
        </ul>
      </div>

      <!-- After -->
      <div class="bg-page-base rounded-2xl border border-primary p-8 shadow-sm">
        <p class="text-xs font-bold text-primary uppercase tracking-widest mb-4">After</p>
        <ul class="space-y-3">
          <li class="flex items-start gap-3">
            <span class="text-primary mt-1">✓</span>
            <span class="text-content-offset">One-click deploys from a portal</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-primary mt-1">✓</span>
            <span class="text-content-offset">Docs live next to services in the catalog</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-primary mt-1">✓</span>
            <span class="text-content-offset">Golden-path templates enforce best practices</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-primary mt-1">✓</span>
            <span class="text-content-offset">Incremental modernization, not big-bang rewrites</span>
          </li>
          <li class="flex items-start gap-3">
            <span class="text-primary mt-1">✓</span>
            <span class="text-content-offset">Clear ownership and dependency maps</span>
          </li>
        </ul>
      </div>
    </div>

    <!-- Image placeholder -->
    <div class="aspect-21/9 bg-page-base rounded-2xl border-2 border-dashed border-trim flex items-center justify-center mb-8">
      <span class="text-content-offset text-sm">Image Placeholder</span>
    </div>

    <div class="text-center">
      <a href="/contact" class="inline-flex items-center gap-2 bg-primary hover:bg-primary-offset text-page-base font-bold px-8 py-4 rounded-full transition-colors text-lg">
        Book a free discovery call
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
      </a>
    </div>
  </div>
</section>
<!--
  IMAGE PROMPT (Variation 2):
  A wide cinematic split-screen illustration (21:9 aspect ratio). Left half shows a
  stressed developer surrounded by sticky notes, multiple terminal windows with errors,
  and a tangle of cloud service logos connected by messy lines. Right half shows the same
  developer relaxed at a clean desk, looking at a polished Backstage UI with a service
  catalog, green deploy status badges, and a clear dependency graph. A glowing vertical
  divider separates the two halves. Color palette: muted grays and reds on the "before"
  side, vibrant teals and greens on the "after" side. Style: modern flat illustration with
  soft shadows, friendly and professional.
-->


<!-- ============================================================
     VARIATION 3: "Features + Stats" dashboard-inspired layout
     ============================================================ -->
<section class="container mx-auto px-4 sm:px-6 lg:px-8 py-16" aria-labelledby="backstage-v3-title">
  <div class="grid grid-cols-1 lg:grid-cols-5 gap-8">
    <!-- Left column: headline + image -->
    <div class="lg:col-span-2 space-y-6">
      <p class="text-xs font-bold text-primary uppercase tracking-widest">Backstage IDP Implementation</p>
      <h2 id="backstage-v3-title" class="text-3xl md:text-4xl font-black text-content leading-tight">
        Give your developers <span class="text-primary">superpowers</span>
      </h2>
      <p class="text-lg text-content-offset leading-relaxed">
        Backstage is Spotify's open-source internal developer portal. I customize and deploy it
        for your team—starting small, proving value fast, then scaling across your organization.
      </p>

      <!-- Image placeholder -->
      <div class="aspect-square bg-page-base-offset rounded-2xl border-2 border-dashed border-trim flex items-center justify-center">
        <span class="text-content-offset text-sm">Image Placeholder</span>
      </div>
    </div>

    <!-- Right column: feature cards + stats -->
    <div class="lg:col-span-3 space-y-6">
      <!-- Stats row -->
      <div class="grid grid-cols-3 gap-4">
        <div class="bg-content text-page-base rounded-xl p-4 text-center">
          <p class="text-3xl font-black">70%</p>
          <p class="text-xs text-page-base/70">Less time to first deploy</p>
        </div>
        <div class="bg-content text-page-base rounded-xl p-4 text-center">
          <p class="text-3xl font-black">4 wks</p>
          <p class="text-xs text-page-base/70">Pilot app live</p>
        </div>
        <div class="bg-content text-page-base rounded-xl p-4 text-center">
          <p class="text-3xl font-black">0</p>
          <p class="text-xs text-page-base/70">Ops tickets for deploys</p>
        </div>
      </div>

      <!-- Feature cards -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div class="bg-page-base-offset p-6 rounded-xl border border-trim">
          <div class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h7"></path></svg>
          </div>
          <p class="font-bold text-content mb-1">Service Catalog</p>
          <p class="text-sm text-content-offset">One place to find every service, API, and resource—with owners and docs attached.</p>
        </div>

        <div class="bg-page-base-offset p-6 rounded-xl border border-trim">
          <div class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path></svg>
          </div>
          <p class="font-bold text-content mb-1">Software Templates</p>
          <p class="text-sm text-content-offset">Scaffold new projects with CI/CD, tests, and cloud infra baked in from day one.</p>
        </div>

        <div class="bg-page-base-offset p-6 rounded-xl border border-trim">
          <div class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
          </div>
          <p class="font-bold text-content mb-1">CI/CD & Cloud Migration</p>
          <p class="text-sm text-content-offset">Lift-and-shift or re-architect—either way, your pipelines will be rock solid.</p>
        </div>

        <div class="bg-page-base-offset p-6 rounded-xl border border-trim">
          <div class="w-10 h-10 bg-primary/10 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path></svg>
          </div>
          <p class="font-bold text-content mb-1">Observability Plug-ins</p>
          <p class="text-sm text-content-offset">Surface logs, metrics, and traces right inside the portal—no context switching.</p>
        </div>
      </div>

      <!-- CTA -->
      <div class="bg-primary/5 border border-primary/20 rounded-xl p-6 flex flex-col sm:flex-row items-center justify-between gap-4">
        <p class="text-content font-medium">Ready to see what Backstage can do for your team?</p>
        <a href="/contact" class="inline-flex items-center gap-2 bg-primary hover:bg-primary-offset text-page-base font-bold px-6 py-3 rounded-full transition-colors whitespace-nowrap">
          Let's talk
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
        </a>
      </div>
    </div>
  </div>
</section>
<!--
  IMAGE PROMPT (Variation 3):
  A square (1:1) hero illustration of a stylized developer portal dashboard. The main focus
  is a Backstage-style UI showing a service catalog with colorful service cards, a sidebar
  navigation, and a prominent "Create New Service" button glowing with an inviting highlight.
  Around the edges, small icons float: a rocket (deploy), a wrench (tooling), a graph
  (observability), and a puzzle piece (plugins). The background is a soft gradient from
  dark navy to deep purple. Style: clean vector art with gentle gradients, rounded corners,
  modern SaaS aesthetic. No photorealism—keep it illustrative and friendly.
-->

</BaseLayout>
