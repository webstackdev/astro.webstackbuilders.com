---
/**
 * Test Navigation component rendered within the real Header component
 * This ensures selector-sync tests use canonical markup.
 */
import Header from '@components/Header/index.astro'

export interface Props {
  path?: string
}

const { path = '/' } = Astro.props
---

<Header path={path} />
