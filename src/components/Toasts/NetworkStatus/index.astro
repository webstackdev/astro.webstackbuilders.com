---
/**
 * NetworkStatus Component
 *
 * Monitors network connectivity and displays toast notifications when
 * connection status changes. Uses the LoadableScript pattern to integrate
 * with Astro View Transitions.
 *
 * @example
 * ```astro
 * <NetworkStatus />
 * ```
 */
import styles from './index.module.css'
---

<network-status-toast
  class:list={['network-status-toast', styles['toastHost']]}
  data-testid="network-status-toast"
></network-status-toast>

<script>
  import { registerNetworkStatusToast } from '@components/Toasts/NetworkStatus/client'
  registerNetworkStatusToast()
</script>