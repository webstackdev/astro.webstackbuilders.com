---
import SearchBar from '@components/SearchBar/index.astro'

const { placeholder = 'Search…', initialQuery = '' } = Astro.props
---

<SearchBar placeholder={placeholder} initialQuery={initialQuery} />
