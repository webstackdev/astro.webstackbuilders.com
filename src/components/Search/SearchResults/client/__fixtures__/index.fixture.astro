---
import SearchResults from '@components/Search/SearchResults/index.astro'

const {
	query = 'typescript',
	limit,
	showSearchBar,
	hideSearchEmptyState,
	resultsMetaOverride,
	syncQueryToLocation,
} = Astro.props
---

<SearchResults
	query={query}
	limit={limit}
	showSearchBar={showSearchBar}
	hideSearchEmptyState={hideSearchEmptyState}
	resultsMetaOverride={resultsMetaOverride}
	syncQueryToLocation={syncQueryToLocation}
/>
