---
/**
 * Sprite component. Place sprite SVG files in `src/assets/images/sprites` to use.
 */
export interface props {
  name: string
}

const { class:className, name } = Astro.props
---
<svg class={className}>
  <use xlink:href=`${Astro.site}assets/images/sprite.svg#${name}`></use>
</svg>