---
import { getMarkerComponent } from '@components/List/server'

export type Props = {
  icon: string
  color?: string
  size?: number
  classes?: string
}

const { icon, color, size, classes }: Props = Astro.props
const MarkerComponent = getMarkerComponent(icon)
---

<MarkerComponent color={color} size={size} classes={classes} />
