---
/**
 * For Window Controls Overlay API, content here shows in
 * PWA title bar area when used as a stand-alone desktop app
 */
---

<div
  class="fixed top-0 left-0 right-0 overflow-hidden z-[9999] bg-[var(--color-bg-offset)]"
  style="height: env(titlebar-area-height, 0);"
>
  <div
    class="absolute flex items-center gap-2 h-full"
    style="left: env(titlebar-area-x, 0); width: env(titlebar-area-width, 100%); -webkit-app-region: drag;"
  >
  </div>
</div>
